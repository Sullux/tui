// Pure layout-geometry calculations
const { resolveMeasure } = require('./types')

const resolveDimension = (dim, maxLimit) => {
  return resolveMeasure(dim, maxLimit)
}

module.exports = {
  resolveDimension,
}

module.exports = {
  resolveDimension,
}
